# Requirements for Google Maps Review Scraper Integration
# Install with: python_env/bin/pip install -r requirements.txt
# Python 3.12+ recommended for best performance

# Core scraping dependencies (updated for Python 3.12+)
scrapy>=2.13.0
scrapy-playwright>=0.0.40
playwright>=1.47.0

# Utilities
python-dateutil>=2.9.0
